import { createTheme } from "@vanilla-extract/css";

export const [themeClass, themeVars] = createTheme({
  colors: {
    teal: "#64b6ac",
    lightBlue: "#c1fdfb",
    white: "#FFFFFF",
    red: "#B6174B",
  },
});
